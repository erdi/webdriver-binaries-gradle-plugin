version: "{build}"
build_script: ./gradlew --no-daemon endToEndTest
install:
  - choco install firefox --version 66.0.3
  - choco install googlechrome --version 87.0.4280.88 --ignore-checksums
  - choco install microsoft-edge --version 108.0.1462.76 --ignore-checksums